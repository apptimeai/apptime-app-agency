import daisyui from 'daisyui';

export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Space Grotesk', 'sans-serif'],
        heading: ['Space Grotesk', 'sans-serif'],
        serif: ['Georgia', 'serif'],
      },
    },
  },
  plugins: [daisyui],
};
